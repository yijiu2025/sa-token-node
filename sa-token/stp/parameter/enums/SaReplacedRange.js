/**
 * SaReplacedRange 枚举类，表示替换范围
 */
const SaReplacedRange = {
    CURR_DEVICE_TYPE: "CURR_DEVICE_TYPE",
    ALL_DEVICE_TYPE: "ALL_DEVICE_TYPE"
};

// 冻结对象以防止修改
Object.freeze(SaReplacedRange);

// 使用 ES6 模块语法导出
export default SaReplacedRange;